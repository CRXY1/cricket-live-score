import express from 'express';
const router = express.Router();

// Placeholder routes - will be implemented
router.get('/', (req, res) => {
  res.json({ success: true, message: 'News routes not implemented yet' });
});

export default router;
